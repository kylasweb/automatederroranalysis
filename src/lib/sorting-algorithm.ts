/**
 * Simple sorting algorithm utility function
 * Converted from Python to TypeScript for serverless deployment
 */

export function sortingAlgorithm<T>(x: T[]): T[] {
    return [...x].sort();
}

export default sortingAlgorithm;
